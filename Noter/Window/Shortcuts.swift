import KeyboardShortcuts

extension KeyboardShortcuts.Name {
    /// Global hotkey that toggles the Noter popup. The user records this in the
    /// onboarding flow and can change it later in Preferences.
    static let toggleNoter = Self("toggleNoter")
}
